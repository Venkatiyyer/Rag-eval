# Core FastAPI requirements (keep all)
uvicorn>=0.15.0
fastapi>=0.68.0
pydantic>=2.0
python-multipart>=0.0.6
python-dotenv>=0.19.0

# PDF processing (only if needed)
PyMuPDF>=1.24.0  # 8MB (keep if using PDF features)

# Whisper (optimized install)
openai-whisper==20231117  # 45MB (slimmer than git install)
torch>=2.0.1,<2.3.0 --extra-index-url https://download.pytorch.org/whl/cpu  # CPU-only (saves ~800MB)

# LangChain core (keep)
langchain>=0.1.0
langchain-groq>=0.1.0  # Groq API essential

# Embeddings (optimized)
sentence-transformers>=2.2.2  # Switch to all-minilm-l6-v2 model (saves ~200MB)

# Audio processing (required)
ffmpeg-python>=0.2.0  # 1MB (keep for whisper)

# Removed to save memory (400MB+ savings)
### huggingface-hub  # Only needed for model downloads
### langchain-community  # Not essential for core functionality
### protobuf  # Usually comes with other deps

# Previously commented (keep commented)
## streamlit
## transformers
## requests
## pysqlite3-binary
## rich
